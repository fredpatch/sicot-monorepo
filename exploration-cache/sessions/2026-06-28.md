# 📅 Session — 2026-06-28

## What Was Done

### 1. exploration-cache Initialized & Committed
Completed the project knowledge base at `exploration-cache/` with full structure.
Commit `43a858d` — 21 files, 2054 insertions.

### 2. OCR Microservice Built (`packages/ocr-service/`)
**Decision**: Python/Flask microservice on port 5001, not npm package.
- `main.py` — Flask + Waitress, `/extract` + `/health` routes
- `requirements.txt` — pdfplumber, pdf2image, pytesseract, python-docx, openpyxl, xlrd, langdetect, Pillow
- Supports: .pdf (native+scanned auto-detection), .docx, .doc (LibreOffice), .txt, .xlsx, .xls, .jpg, .png, .tiff
- Text cleanup: apostrophe normalization (`"l ' annexe"` → `"l'annexe"`)
- Tested on real ANAC documents ✅

### 3. `packages/server/src/utils/ocr.ts` Created
TypeScript HTTP client for the OCR service:
- `extraireTexte({ buffer, nomFichier, mimeType })` — POST /extract via form-data
- `verifierServiceOCR()` — GET /health check (called at server startup)
- Error codes: `OCR_SERVICE_INDISPONIBLE`, `OCR_TIMEOUT`, `OCR_ERREUR`
- 60s timeout

### 4. Server Updated
- `packages/server/src/index.ts` — added OCR health check at startup (warns if unavailable, doesn't crash)
- `packages/server/package.json` — added `axios`, `form-data`, `@types/form-data`

### 5. LibreTranslate Validated
FR↔EN quality acceptable for V1. Known issue: spaces around apostrophes — fixed in OCR cleanup.

## Key Decisions Made

- ✅ **OCR = Python microservice** (not npm). Reasons: better format support (.doc via LibreOffice, native PDF detection), more mature libraries, easier to maintain separately from Node.
- ✅ **PDF strategy**: pdfplumber first (native), fall back to Tesseract per page if text is empty (scanned)
- ✅ **Apostrophe cleanup** in `nettoyer_texte()` — applied to all extracted text before returning

## State at End of Session

- Commits: `43a858d` (exploration-cache), `5d193f5` (OCR microservice)
- Branch: `main`
- Next: Module Organisations (M2) server, then client pages

---

### Part 2 — Documents Module & Path Alias (same day, continued)

#### 4. Documents Service Split
`documents.service.ts` (405 lines) split into 4 focused files:
- `documents.types.ts` — interfaces + `DocumentCategorie` union
- `documents.constants.ts` — `UPLOAD_DIR`, `DOSSIERS`, `MOTS_CLES_CATEGORIES`
- `documents.helpers.ts` — `assurerDossiers`, `toDocumentView`, `genererNomFichier`, `classerAutomatiquement`
- `documents.service.ts` — service functions only

#### 5. Documents Controller Split
Extracted `documents.errors.ts` with `handleDocumentsError` + error map (reusable across future controllers).

#### 6. Upload Middleware
Created `packages/server/src/middleware/upload.ts`:
- `multer.memoryStorage()` — buffer direct to OCR, no temp file
- MIME-type whitelist (9 types: PDF, DOC, DOCX, XLS, XLSX, TXT, JPEG, PNG, TIFF)
- 50MB limit
- `handleMulterError` — proper Express error middleware with typed params

#### 7. Documents Route Written & Mounted
`documents.route.ts` written from scratch and mounted in `index.ts` at `/api/documents`.
Route ordering: `/doublon` declared before `/:id` to avoid routing collision.

#### 8. Server `@/` Path Alias
- `tsconfig.json`: added `baseUrl: "./src"`, `paths: { "@/*": ["./*"] }`
- All 30 relative cross-module imports across 15 files migrated to `@/`
- `tsc-alias` added to devDeps + `build` script (`tsc && tsc-alias`) for production support

## Notes for Next Session

1. Documents server API is complete — start with Module Organisations (M2)
2. See `active-session/next-actions.md` for exact scaffold steps
3. `@/` path alias is live — use it for all new server imports
