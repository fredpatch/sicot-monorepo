# 📅 Session — 2026-06-28

## What Was Done

### 1. exploration-cache Initialized & Committed
Completed the project knowledge base at `exploration-cache/` with full structure.
Commit `43a858d` — 21 files, 2054 insertions.

### 2. OCR Microservice Built (`packages/ocr-service/`)
**Decision**: Python/Flask microservice on port 5001, not npm package.
- `main.py` — Flask + Waitress, `/extract` + `/health` routes
- `requirements.txt` — pdfplumber, pdf2image, pytesseract, python-docx, openpyxl, xlrd, langdetect, Pillow
- Supports: .pdf (native+scanned auto-detection), .docx, .doc (LibreOffice), .txt, .xlsx, .xls, .jpg, .png, .tiff
- Text cleanup: apostrophe normalization (`"l ' annexe"` → `"l'annexe"`)
- Tested on real ANAC documents ✅

### 3. `packages/server/src/utils/ocr.ts` Created
TypeScript HTTP client for the OCR service:
- `extraireTexte({ buffer, nomFichier, mimeType })` — POST /extract via form-data
- `verifierServiceOCR()` — GET /health check (called at server startup)
- Error codes: `OCR_SERVICE_INDISPONIBLE`, `OCR_TIMEOUT`, `OCR_ERREUR`
- 60s timeout

### 4. Server Updated
- `packages/server/src/index.ts` — added OCR health check at startup (warns if unavailable, doesn't crash)
- `packages/server/package.json` — added `axios`, `form-data`, `@types/form-data`

### 5. LibreTranslate Validated
FR↔EN quality acceptable for V1. Known issue: spaces around apostrophes — fixed in OCR cleanup.

## Key Decisions Made

- ✅ **OCR = Python microservice** (not npm). Reasons: better format support (.doc via LibreOffice, native PDF detection), more mature libraries, easier to maintain separately from Node.
- ✅ **PDF strategy**: pdfplumber first (native), fall back to Tesseract per page if text is empty (scanned)
- ✅ **Apostrophe cleanup** in `nettoyer_texte()` — applied to all extracted text before returning

## State at End of Session

- Commits: `43a858d` (exploration-cache), then OCR service files (unstaged at end)
- Branch: `main`
- Next: commit OCR service + server changes, then `modules/documents/` scaffold

## Notes for Next Session

1. Check `git status` — `packages/ocr-service/`, `utils/ocr.ts`, `server/index.ts`, `server/package.json` need staging
2. Add `packages/ocr-service/.venv/` to `.gitignore` before committing
3. Start `modules/documents/` — see `active-session/next-actions.md` for exact steps
4. OCR service must be started separately: `cd packages/ocr-service && .venv\Scripts\activate && python main.py`
