# 📅 Session — 2026-06-27

## What Was Done

### 1. `packages/client/src/lib/api.ts` Split
Original monolithic file replaced by:
- `lib/axios.ts` — Axios instance with 401 interceptor + refresh queue
- `lib/auth.api.ts` — `login()`, `setPassword()`, `refresh()`, `logout()`, `me()`
- `lib/users.api.ts` — user CRUD + activation + reinitOTP
- `lib/audit.api.ts` — lister, getById, getModules, getActions
- `lib/api.ts` — barrel re-export of all above

### 2. LoginPage Redesign (framer-motion + shadcn components)
Full redesign of `LoginPage.tsx` using `/ui-ux-pro-max` and `/frontend-design` skills:
- 2-step animated flow with directional slides (`AnimatePresence` + `slideVariants`)
- OTP / password mode switcher
- Password strength meter (4 bars + criteria checklist)
- Eye toggle on password inputs
- Server error banner with accessibility attributes
- Grid pattern SVG background

### 3. UI Components Created (shadcn-style, manual)
- `components/ui/button.tsx` — CVA, 5 variants, 4 sizes
- `components/ui/input.tsx` — forwardRef
- `components/ui/label.tsx` — forwardRef
- `lib/utils.ts` — `cn()` helper

### 4. LoginPage Split Into Co-Located Folder
Extracted to `pages/login/`:
- `schemas.ts` — Zod discriminatedUnion schema
- `animations.ts` — framer-motion constants
- `components/` — 7 sub-components (FormField, PasswordStrength, EyeToggle, ServerError, StepTab, ModeTab, GridPattern)

### 5. Path Alias `@/` Added
- `vite.config.ts`: `resolve.alias: { '@': path.resolve(__dirname, './src') }`
- `tsconfig.json`: `baseUrl: '.'`, `paths: { '@/*': ['./src/*'] }`

### 6. Commit & Push
Commit `d51eee7` pushed to `origin/main` — 27 files, 1441 insertions.

## Problems Encountered

### framer-motion TypeScript errors (45 min)
`ease: [bezier array]` and `ease: 'easeOut'` both rejected inside `Variants` objects.
**Solution**: Extract `slideTx: Transition` constant, pass via `transition` prop on `motion.div`.

### `tsconfig.base.json ignoreDeprecations TS5103` (15 min)
`"ignoreDeprecations": "6.0"` in root base config triggers `TS5103`.
**Solution**: Remove from base, keep only in `packages/client/tsconfig.json`.

### shadcn CLI corrupts Tailwind v4 (discovered, not experienced)
Investigated shadcn CLI usage. Determined it's incompatible with Tailwind v4.
**Solution**: Manual component creation.

## Decisions Made

- ✅ No shadcn CLI — manual components
- ✅ framer-motion transitions always on prop, never in Variants
- ✅ `ignoreDeprecations` stays in client tsconfig only
- ✅ LoginStep + SetPasswordStep remain inline in LoginPage.tsx (too coupled to extract)

## Files Changed

27 files total. See commit `d51eee7` or `tasks/completed/sprint-1-done.md` for full list.
